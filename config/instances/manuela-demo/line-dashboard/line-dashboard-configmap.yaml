apiVersion: v1
kind: ConfigMap
metadata:
  name: line-dashboard-configmap
data:
  config.json: |-
    {
        "websocketHost": "http://messaging-manuela-demo-messaging.OCP_WILDCARD_DOMAIN",
        "websocketPath": "/api/service-web/socket",
        "SERVER_TIMEOUT": 20000
    }